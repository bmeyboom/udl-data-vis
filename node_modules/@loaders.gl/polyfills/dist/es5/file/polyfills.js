"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.installFilePolyfills = installFilePolyfills;

var _readableStreamPolyfill = require("./readable-stream-polyfill");

var _blobPolyfill = require("./blob-polyfill");

var _fileReaderPolyfill = require("./file-reader-polyfill");

var _filePolyfill = require("./file-polyfill");

function installFilePolyfills() {
  if (typeof ReadableStream === 'undefined' && global) {
    global.ReadableStream = _readableStreamPolyfill.ReadableStreamPolyfill;
  }

  if (typeof Blob === 'undefined' && global) {
    global.Blob = _blobPolyfill.BlobPolyfill;
  }

  if (typeof FileReader === 'undefined' && global) {
    global.FileReader = _fileReaderPolyfill.FileReaderPolyfill;
  }

  if (typeof File === 'undefined' && global) {
    global.File = _filePolyfill.FilePolyfill;
  }
}
//# sourceMappingURL=polyfills.js.map